const initialState = {
  loggedIn: true,
  isLoading: false,
  error: null,
}

export default function tasks(state = initialState, action) {
  switch (action.type) {
    default: {
      return state
    }
  }
}
